import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    brand: {
      primario: '#000000',
      secundario: '#ffffff',
      terciario: '#484848',
    },
  },
})

export default theme
